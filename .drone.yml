pipeline:
  deploy:
    image: marcelsud/kubectl
    commands:
      - echo $KUBECONFIG_DATA > /tmp/kubeconfig
      - kubectl get pods --namespace=default --kubeconfig=/tmp/kubeconfig
    secrets:
      - kubeconfig_data

#   publish-push:
#     image: plugins/docker
#     repo: marcelsud/demo
#     tags:
#       - ${DRONE_COMMIT_BRANCH}
#     force_tag: true
#     secrets: [ docker_username, docker_password ]
  
#       - kubectl set image deployment.apps/demo demo=marcelsud/demo:${DRONE_COMMIT_BRANCH} --namespace=default --kubeconfig=/root/.kube/config  
